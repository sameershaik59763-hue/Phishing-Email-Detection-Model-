# Phishing-Email-Detection-Model-
This project builds a machine learning model using Scikit-learn to detect phishing emails. It analyzes email text, URLs, and key features to classify messages as phishing or safe. The model is trained on labeled data and evaluated using accuracy and confusion matrix for reliable prediction.
